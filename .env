# REST API
API_BASE_URL=https://api.gekychat.com/api/v1

# Reverb (self-hosted) — must match server REVERB_* in gekychat/.env.example
REVERB_HOST=chat.gekychat.com
REVERB_PORT=443
REVERB_SCHEME=https

PUSHER_HOST=chat.gekychat.com
PUSHER_APP_KEY=gk-reverb-key-2024
PUSHER_WSS_PORT=443
PUSHER_FORCE_TLS=true

# Clients connect via WSS on 443 — do NOT use REVERB_SERVER_PORT (8080) here.
# PUSHER_WS_PORT=8080

PUSHER_AUTH_ENDPOINT=https://api.gekychat.com/api/v1/broadcasting/auth

# Legacy Pusher Cloud entries (not used while PUSHER_HOST / REVERB_HOST is set):
# PUSHER_KEY=lcrpz9jk9d585ezgg16s
# PUSHER_APP_ID=2093574
# PUSHER_APP_KEY=6481ff66c09182cc96cb
# PUSHER_CLUSTER=mt1
# PUSHER_APP_CLUSTER=mt1
